"""scrollback: navigate, search, copy, and export AI coding-agent sessions.

A local-first, read-only tool that reads session history from multiple AI
coding agents (opencode, Claude Code, ...) through a common adapter
interface and lets you list, view, search, and export those sessions.
"""

__version__ = "0.4.0"
